// This file is the legacy entry point.
// The actual entry point is src/backend/extension.ts (configured in esbuild.js).
// Re-exporting for compatibility.
export { activate, deactivate } from './backend/extension';